let mapleader = ","

" copy `bundle exec rspec spec/current_file_spec.rb` to clipboard
nnoremap <Leader>r :call CopyRSpecCommand()<CR>

" copy `bundle exec rspec spec/current_file_spec.rb -l <current-line>` to clipboard
nnoremap <Leader>R :call CopyRSpecCommand(1)<CR>

" then use cmd-t to switch over to terminal and paste it in.
" you don't even have to leave the keyboard :)
